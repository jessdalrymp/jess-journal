
// This is a temporary placeholder file to prevent build errors
// The mood functionality has been removed

export function useMoodActions() {
  return {
    addMoodEntry: () => Promise.resolve(null),
    fetchMoodEntries: () => Promise.resolve([]),
  };
}
